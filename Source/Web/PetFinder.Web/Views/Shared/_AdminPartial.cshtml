@using Microsoft.AspNet.Identity
@using PetFinder.Common.Constants

@if (this.User.Identity.IsAuthenticated && this.User.IsInRole(Models.AdminRole))
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Администрация
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
            <li>@Html.ActionLink("Потребители", "Index", "Users", new { area = Others.AdminArea }, null)</li>
            <li>@Html.ActionLink("Коментари", "Index", "Comments", new { area = Others.AdminArea }, null)</li>
            <li>@Html.ActionLink("Животни", "Index", "Pets", new { area = Others.AdminArea }, null)</li>
        </ul>
    </li>
}
